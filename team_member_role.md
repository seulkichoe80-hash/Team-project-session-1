# 📘 [초보자 완벽 가이드] 노코드 AI 뉴스 자동화 파이프라인 팀원별 상세 안내서 (R&R)

> **이 문서의 목적**: AI나 Make.com, Notion 등 지원 도구를 한 번도 사용해 본 적 없는 초보 사용자도 그대로 따라 하기만 하면 뉴스 수집부터 AI 요약, 썸네일 생성, 감성 분석, Notion DB 저장, 에러 처리까지 완벽하게 구축할 수 있도록 제작된 **100% 실습형 매뉴얼**입니다.

---

## 🔰 1. 초보자를 위한 1분 기초 개념 사전

실습을 시작하기 전, 프로젝트에서 자주 등장하는 핵심 용어를 비유와 함께 쉬운 언어로 알아봅니다.

| 용어 | 개념 설명 | 쉬운 비유 |
| :--- | :--- | :--- |
| **Make.com** | 코딩 없이 여러 웹 서비스를 연결하여 자동으로 일하게 만드는 '노코드 자동화 도구' | 여러 기능 블록을 이어 붙이는 **디지털 레고 조립판** |
| **모듈 (Module)** | Make.com 안에서 특정 기능(뉴스 가져오기, AI 요약하기 등)을 수행하는 하나의 조립 블록 | 레고 조립판 위에 올려놓는 **개별 레고 블록** |
| **RSS 피드** | 뉴스 웹사이트에서 새로운 기사가 올라오면 알려주는 구독용 전용 주소 | 매일 아침 집 앞으로 신문이 배달되는 **디지털 신문 배달 주소** |
| **OpenAI API Key** | Make.com이 ChatGPT나 DALL-E 인공지능을 가져다 쓸 수 있게 해주는 개인 접속 비밀번호 | AI 서비스를 이용하기 위한 **비밀 열쇠** |
| **프롬프트 (Prompt)** | AI(ChatGPT/DALL-E)에게 작업 내용과 형식을 전달하는 지시문 또는 질문 | AI 직원에게 전달하는 **작업 지시서** |
| **매핑 (Mapping)** | 이전 블록에서 나온 결과물(기사 제목, 내용 등)을 다음 블록의 입력 칸에 연결하는 작업 | 이전 단계 부품을 다음 단계 입력 구멍에 **쏙 집어넣는 연결 작업** |
| **Notion 데이터베이스** | 수집 및 요약된 뉴스를 표나 카드로 깔끔하게 정리해 모아두는 곳 | 결과를 차곡차곡 정리하는 **디지털 스크랩북** |

---

## 🔄 2. 전체 자동화 파이프라인 흐름

전체 자동화는 다음과 같이 5명의 팀원이 한 단계씩 맡아 순차적으로 이어지는 파이프라인 구조입니다.

```text
[팀원 1: 김승현]           [팀원 2: 이태우]            [팀원 3: 한승학]
 1. RSS 뉴스 수집   ──►   2. OpenAI 3줄 요약  ──►   3. DALL-E 썸네일 생성
 2. 키워드 필터링            프롬프트 작성             + 감성 분석 태그
                                                                │
[팀원 5: 최슬기]           [팀원 4: 공미순]                      │
 5. 총괄 QA 검증    ◄──   4. Notion DB에 저장  ◄───────────────┘
    & README 작성          + 에러 핸들러 구성
```

---

## 👥 3. 팀원별 담당 역할 및 1:1 따라하기 매뉴얼

---

### 👤 팀원 1: 김승현 - 데이터 수집 및 조건 필터링 (Data Ingestion & Preprocessing)

> **핵심 미션**: 매일 정해진 시간에 뉴스를 수집하고, 수집된 뉴스 중 'AI' 관련 기사만 걸러내어 다음 단계로 넘깁니다.

#### 🛠️ 1단계: Make.com 계정 준비 및 새 시나리오 생성
1. 인터넷 브라우저(Chrome 권장)를 열고 [Make.com](https://www.make.com/)에 접속합니다.
2. 우측 상단의 **[Sign up]** 또는 **[Log in]**을 눌러 계정을 생성하거나 로그인합니다.
3. 로그인 후 메인 대시보드 화면의 왼쪽 메뉴나 우측 상단에 있는 **[Create a new scenario]** (새 시나리오 생성) 버튼을 클릭합니다.
4. 하얀 바탕의 넓은 작업 공간(캔버스)이 뜨고 중앙에 **플러스(+) 버튼이 있는 커다란 둥근 원**이 나타납니다.

#### 🛠️ 2단계: 자동 실행 스케줄러(Schedule) 설정
1. 커다란 플러스(+) 원 옆이나 아래에 있는 **시계 모양 아이콘**을 클릭합니다.
2. **Run scenario** (시나리오 실행 주기) 메뉴에서 **`Every day`** (매일)를 선택합니다.
3. **Time** 항목에 `09:00`을 입력하여 매일 아침 9시에 실행되도록 설정합니다.
4. 하단의 파란색 **[OK]** 버튼을 클릭합니다.

#### 🛠️ 3단계: RSS 뉴스 수집 모듈 추가 및 주소 설정
1. 중앙의 **플러스(+) 버튼**을 클릭하여 모듈 검색창을 엽니다.
2. 검색창에 **`RSS`**를 입력하고 검색 결과에서 주황색 **[RSS]** 아이콘을 클릭합니다.
3. 세부 작업 선택 메뉴가 나오면 **`Retrieve RSS feed items`** (RSS 피드 항목 가져오기)를 클릭합니다.
4. 캔버스에 주황색 RSS 모듈이 추가됩니다. 모듈을 **더블클릭**하여 설정 창을 엽니다.
5. **URL** 입력 칸에 수집할 연합뉴스 피드 주소를 복사하여 붙여넣습니다.
   - 📌 **사용할 주소**: `https://www.yna.co.kr/rss/news.xml` (연합뉴스 최신기사 RSS)
6. **Maximum number of returned items** (가져올 최대 기사 수) 항목에 **`1`**을 입력합니다. (테스트 및 비용 절감을 위해 매번 최신 기사 1건만 가져옵니다.)
7. 하단의 파란색 **[OK]** 버튼을 클릭해 닫습니다.

#### 🛠️ 4단계: 'AI' 주제 기사만 통과시키는 조건 필터(Filter) 설정
1. RSS 모듈의 오른쪽 끝에 마우스를 올리면 작은 **플러스(+) 모양 연결선**이나 **스패너(렌치) 아이콘**이 보입니다.
2. 모듈과 모듈 사이를 연결하는 **점선/실선(연결선)**을 클릭합니다.
3. 팝업창이 뜨면 **[Set up a filter]** (필터 설정)를 클릭합니다.
4. 설정 칸에 다음 내용을 정확히 입력합니다:
   - **Label** (필터 이름): `AI 키워드 필터링`
   - **Condition** (조건 설정):
     - 첫 번째 왼쪽 입력 칸을 클릭하면 이전 RSS 모듈에서 가져온 데이터 변수 목록 패널이 뜹니다.
     - 패널에서 **`Title`** (기사 제목) 항목을 찾아 클릭하여 넣어줍니다.
     - 중앙 조건 선택 드롭다운 메뉴에서 **`Contains (case insensitive)`** (포함 - 대소문자 구문 없음)를 선택합니다.
     - 오른쪽 입력 칸에 **`AI`** 라고 입력합니다.
5. (선택사항) '인공지능'이나 'GPT'도 포함하고 싶다면 아래 **[Add OR rule]** 버튼을 누르고 `Title` - `Contains` - `인공지능`을 추가합니다.
6. 하단의 파란색 **[OK]** 버튼을 눌러 필터 설정을 완료합니다.

#### ⚠️ 팀원 1이 주의할 점:
- RSS URL 주소 앞뒤에 공백(띄어쓰기)이 들어가면 에러가 발생하니 피드 주소만 정확히 붙여넣으세요.
- 필터 조건이 너무 까다로우면 통과하는 뉴스가 없어 다음 단계가 실행되지 않습니다. 처음에는 `AI` 하나만 넣고 테스트하세요.

---

### 👤 팀원 2: 이태우 - AI 요약 & 텍스트 가공 (AI Core Processing)

> **핵심 미션**: OpenAI API를 연결하고, 수집된 뉴스 기사의 본문을 읽어 핵심만 3줄로 정확하게 요약하도록 프롬프트를 구성합니다.

#### 🛠️ 1단계: OpenAI API Key 발급받기
1. 인터넷 브라우저로 [OpenAI Platform](https://platform.openai.com/)에 접속하여 가입 및 로그인합니다.
2. 좌측 메뉴바에서 **[API keys]** 아이콘(열쇠 모양)을 클릭하거나, 오른쪽 상단 프로필 클릭 후 **[View API keys]**를 선택합니다.
3. 화면 중앙의 **[+ Create new secret key]** (새 비밀 키 생성) 버튼을 누릅니다.
4. Key 이름(Name)에 `Make-Automation` 등을 입력하고 **[Create secret key]**를 클릭합니다.
5. 화면에 `sk-proj-...`로 시작하는 긴 문자열이 나타납니다. **[Copy]** 버튼을 눌러 복사한 뒤, **즉시 메모장에 붙여넣어 안전하게 저장**합니다. (팝업창을 닫으면 키를 다시 볼 수 없습니다.)

#### 🛠️ 2단계: Make.com에 OpenAI 모듈 추가 및 Connection 연동
1. 팀원 1의 RSS 필터 오른쪽에 위치한 **[Add a module]** (플러스 아이콘)을 누릅니다.
2. 검색창에 **`OpenAI`**를 검색하여 선택합니다.
3. 작업 목록 중 **`Create a Completion (Prompt Output)`** 또는 **`Create a Chat Completion`**을 클릭합니다.
4. 모듈 설정창 상단의 **Create a Connection** (연결 생성) 버튼을 누릅니다.
5. Connection name에 `My OpenAI`를 적고, **API Key** 입력란에 방금 메모장에 저장해 둔 `sk-proj-...` 비밀 키를 붙여넣은 뒤 **[Save]**를 누릅니다.

#### 🛠️ 3단계: AI 요약 프롬프트 입력 및 데이터 매핑
1. **Model** (AI 모델) 선택란에서 **`gpt-4o-mini`** (또는 `gpt-3.5-turbo`)를 선택합니다.
2. **Messages** (메시지 목록) 항목에서 **[Add item]**을 누르고 역할 및 지시문을 설정합니다:

   - **Item 1 (시스템 지시문)**:
     - **Role**: `System`
     - **Message Content**:
       ```text
       당신은 전문 한국어 뉴스 요약 에이전트입니다. 전달받은 뉴스 기사의 제목과 본문을 분석하여 핵심 내용을 정확히 3줄로 요약하세요. 불필요한 서론이나 인사말은 제외하고 오직 요약문만 출력하세요.
       ```

   - **Item 2 (사용자 요청문 & 데이터 매핑)**:
     - **Role**: `User`
     - **Message Content**:
       ```text
       다음 기사를 3줄로 요약해 주세요.

       [기사 제목]: 
       [기사 본문]: 
       ```
     - 📌 **변수 매핑 방법**:
       - `[기사 제목]:` 뒤에 커서를 두고, 클릭 시 나타나는 마우스 팝업 패널에서 **RSS 모듈의 `Title` 변수**를 선택해 넣습니다.
       - `[기사 본문]:` 뒤에 커서를 두고 패널에서 **RSS 모듈의 `Description` (또는 Summary) 변수**를 선택해 넣습니다.

3. 하단의 파란색 **[OK]** 버튼을 눌러 저장을 완료합니다.

#### ⚠️ 팀원 2가 주의할 점:
- OpenAI API는 사용량이 적어도 결제 수단이 등록되어 있거나 프리트라이얼 크레딧이 남아있어야 정상 작동합니다. (신규 계정 가입 시 제공되는 기본 크레딧 확인 필요)
- 프롬프트에 `[기사 제목]`과 `[기사 본문]` 변수가 올바르게 태그 모양(주황색 RSS 블록 표기)으로 들어갔는지 반드시 확인하세요.

---

### 👤 팀원 3: 한승학 - 보너스 기능 개발 (DALL-E 썸네일 & 감성 분석)

> **핵심 미션**: 뉴스 기사의 분위기에 어울리는 이미지(썸네일)를 AI로 만들고, 기사의 감성(긍정/부정/중립)을 분석하는 태그를 추출합니다.

#### 🛠️ 1단계: DALL-E 3 이미지 생성 모듈 설정
1. 팀원 2의 OpenAI 요약 모듈 오른쪽에 **[Add a module]** 버튼을 누릅니다.
2. **`OpenAI`** 모듈을 선택하고, 작업 메뉴에서 **`Generate an Image`** (이미지 생성)를 선택합니다.
3. **Connection**: 팀원 2가 미리 만들어 둔 OpenAI 연결을 동일하게 선택합니다.
4. **Model**: **`dall-e-3`** (또는 `dall-e-2`)를 선택합니다.
5. **Prompt** (이미지 생성 지시문) 입력란에 다음 텍스트를 입력하고 변수를 매핑합니다:
   ```text
   Create a professional technology news thumbnail image inspired by the title: {{RSS Title}}. High quality, modern vector illustration style, blue and white background, sleek design. No text in the image.
   ```
   *(※ `{{RSS Title}}` 부분은 마우스 클릭 패널에서 RSS 모듈의 **`Title`** 변수를 선택해 넣습니다.)*
6. **Size**: `1024x1024` 선택 후 **[OK]**를 눌러 닫습니다.

#### 🛠️ 2단계: 뉴스 감성 분석(Sentiment Analysis) 모듈 설정
1. 이미지 모듈 오른쪽에 모듈을 하나 더 추가하고 **`OpenAI`** -> **`Create a Chat Completion`**을 선택합니다.
2. **Model**: `gpt-4o-mini` 선택.
3. **Messages** 추가:
   - **Role**: `System`
   - **Content**: `당신은 감성 분석 전문가입니다. 제공된 뉴스 내용을 읽고 긍정, 부정, 중립 중 딱 하나의 단어로만 답변하세요.`
   - **Role**: `User`
   - **Content**: `다음 기사의 감성을 분류해줘: {{RSS Title}} {{RSS Description}}`
4. 파란색 **[OK]**를 누릅니다.

#### ⚠️ 팀원 3이 주의할 점:
- DALL-E 3 이미지 생성은 건당 약 $0.04의 비용이 발생하므로 테스트 시에는 1~2회만 실행하세요.
- 감성 분석 결과는 노션의 '선택(Select)' 속성에 들어가므로, AI 출력이 '긍정', '부정', '중립' 외의 딴소리(예: "이 기사의 감성은 긍정입니다.")를 하지 못하도록 프롬프트를 엄격히 설정해야 합니다.

---

### 👤 팀원 4: 공미순 - Notion DB 구축 & 에러 핸들링 (Storage & Reliability)

> **핵심 미션**: 자동화 결과를 정리할 Notion 데이터베이스를 만들고 Make.com과 연결한 뒤, 에러 발생 시 시스템이 멈추지 않도록 예외 처리(Error Handler)를 구성합니다.

#### 🛠️ 1단계: Notion 데이터베이스 페이지 생성
1. 웹 브라우저에서 [Notion.so](https://www.notion.so/)에 접속하여 로그인합니다.
2. 왼쪽 사이드바에서 **[+ 페이지 추가]**를 클릭합니다.
3. 페이지 제목에 `🤖 AI 뉴스 자동 요약 보관함`을 입력합니다.
4. 본문 빈 곳에 `/데이터베이스 - 전체 페이지` (또는 `/Database - Full page`)를 입력하고 엔터를 쳐서 데이터베이스를 만듭니다.
5. 데이터베이스 표의 열(속성)을 다음 표와 동일하게 **이름과 유형**을 정확히 설정합니다:

| 속성 이름 (Title) | 속성 유형 (Property Type) | 설명 |
| :--- | :--- | :--- |
| **제목** (기본 이름) | **제목 (Title)** | 뉴스 기사 제목 |
| **요약** | **텍스트 (Text)** | AI가 생성한 3줄 요약 문장 |
| **원문 링크** | **URL** | 뉴스 기사 실제 웹 주소 |
| **발행일시** | **날짜 (Date)** | 기사 수집/발행 시간 |
| **감성 태그** | **선택 (Select)** | 옵션: `긍정`, `부정`, `중립` 미리 등록해두기 |
| **썸네일** | **URL** | DALL-E가 생성한 이미지 주소 |

#### 🛠️ 2단계: Make.com Notion 모듈 연결 및 데이터 매핑
1. Make.com 캔버스 파이프라인 제일 끝에 **[Add a module]**을 누르고 **`Notion`**을 검색해 선택합니다.
2. 작업 목록 중 **`Create a Database Item`** (데이터베이스 항목 생성)을 선택합니다.
3. **Connection** -> **[Create a Connection]**을 클릭합니다.
4. 노션 로그인 창이 뜨면 권한 요청 화면에서 방금 만든 `🤖 AI 뉴스 자동 요약 보관함` 페이지 권한 체크 후 **[페이지 허용 / Allow access]**를 누릅니다.
5. **Database ID** 선택란에서 `🤖 AI 뉴스 자동 요약 보관함`을 선택합니다.
6. 아래에 나타나는 노션 속성 칸에 이전에 생성된 데이터 변수들을 1:1로 클릭하여 매핑합니다:
   - **제목 (Title)** ──► RSS 모듈의 **`Title`**
   - **요약 (Text)** ──► 팀원 2 (OpenAI 요약) 모듈의 **`Result / Text`** (또는 `Choices[].message.content`)
   - **원문 링크 (URL)** ──► RSS 모듈의 **`URL`** (또는 `Link`)
   - **발행일시 (Date)** ──► RSS 모듈의 **`Date`** (또는 Make의 `now` 변수)
   - **감성 태그 (Select)** ──► 팀원 3 (감성 분석) 모듈의 **`Result / Text`**
   - **썸네일 (URL)** ──► 팀원 3 (DALL-E 이미지) 모듈의 **`Data[].url`**
7. **[OK]**를 눌러 완료합니다.

#### 🛠️ 3단계: 에러 핸들러(Error Handler) 설정
1. OpenAI 모듈이나 Notion 모듈 위에서 **마우스 우클릭**을 합니다.
2. 메뉴에서 **[Add error handler]** (에러 핸들러 추가)를 클릭합니다.
3. 에러 발생 시 처리할 방식으로 **`Ignore`** (오류 무시하고 계속 진행) 또는 **`Break`** (일시 보관 후 재시도)를 선택합니다.
4. (선택사항) 에러 시 나에게 알려주고 싶다면 에러 경로 뒤에 **`Email`** 또는 **`Gmail`** 모듈을 붙여 `Send an email`로 에러 메시지 알림을 발송하도록 설정할 수 있습니다.

#### ⚠️ 팀원 4가 주의할 점:
- 노션 데이터베이스 속성의 '이름'이나 '유형(Type)'이 Make.com 매핑 화면과 다르면 데이터 저장이 안 되고 빨간색 오류가 뜹니다.
- 노션 API 연결 시 새로 만든 페이지 권한이 연결 목록에 제대로 포함되었는지 반드시 확인하세요.

---

### 👤 팀원 5: 최슬기 - 총괄 QA, E2E 테스트 & 최종 문서화 (QA & Technical Writer)

> **핵심 미션**: 파이프라인 전체가 정상 동작하는지 테스트(Run Once)하고, 각 모듈의 데이터 흐름을 점검하며 최종 결과물(README)을 정돈합니다.

#### 🛠️ 1단계: End-to-End 전체 시연 테스트 (Run Once)
1. Make.com 화면 좌측 하단 조종판에 있는 재생 모양의 **[Run Once]** (한 번 실행) 버튼을 클릭합니다.
2. 각 모듈이 주황색/파란색 휠을 돌며 순서대로 실행되는 모습을 관찰합니다.
3. 수초 후 모듈 우측 상단에 숫자가 적힌 **둥근 노란색/파란색 말풍선(Inspector, 인스펙터 `①`, `②` 등)**이 나타나고 모두 체크표시(✔)가 뜨는지 확인합니다.

#### 🛠️ 2단계: 모듈별 인스펙터(Inspector) 클릭 및 검증
1. **RSS 모듈 말풍선 클릭**: `Output` -> `Bundle 1`을 열어 뉴스 제목과 내용이 잘 들어왔는지 확인합니다.
2. **필터 점검**: 필터 연결선 위에 마우스를 올려 뉴스 제목에 'AI' 키워드가 없어서 걸러졌는지, 통과했는지 확인합니다.
3. **OpenAI 모듈 말풍선 클릭**: `Output` -> `Choices` -> `Message` -> `Content`를 열어 3줄 요약 문장이 깨지지 않고 잘 생성되었는지 검증합니다.
4. **Notion 실제 페이지 접속**: 노션 데이터베이스 표에 새로운 행이 생기면서 **제목, 요약, 링크, 날짜, 감성태그, 썸네일 URL**이 깔끔하게 채워졌는지 눈으로 직접 확인합니다.

#### 🛠️ 3단계: 최종 제출 문서(README.md) 정리 및 API Key 마스킹
1. 팀원들이 사용한 RSS 피드 주소, 프롬프트 문구, 노션 DB 구조를 종합 정리합니다.
2. 캡처 화면이나 텍스트 캡처 내에 **OpenAI API Key(`sk-proj-...`)** 나 개인 노션 계정 이메일 등 **보안 민감 정보가 노출되어 있는지 확인하고 마스킹(모자이크/삭제) 처리**합니다.

---

## 🛠️ 4. 지원 도구별 화면 위치 & 1:1 클릭 가이드

### 4-1. Make.com 캔버스 조작 가이드
- **모듈 추가**: 캔버스 빈 곳 클릭 후 하단 메뉴의 `+ (Add)` 버튼 클릭.
- **모듈 연결**: 모듈 오른쪽 끝의 둥근 고리 아이콘을 드래그하여 다른 모듈에 가져다 대면 화살표 연결.
- **데이터 매핑 패널**: 입력 칸에 마우스 커서를 놓으면 화면 옆에 나타나는 이전 모듈 변수 선택창.

### 4-2. OpenAI API Key 및 결제 확인 가이드
- **접속 주소**: [https://platform.openai.com/api-keys](https://platform.openai.com/api-keys)
- **크레딧 잔액 확인**: 좌측 메뉴 `Settings` -> `Billing` 에서 남아있는 잔액($) 확인.

### 4-3. Notion API 연결 승인 가이드
- Make.com에서 Notion 연결 생성 시 팝업 창 하단의 `[Select pages]` (페이지 선택)를 눌러 뉴스 보관함 페이지가 선택되어 있는지 확인 후 `[Allow access]` 클릭.

---

## ❓ 5. 초보자가 자주 겪는 5가지 문제 & 트러블슈팅 FAQ

### Q1. `Run Once`를 눌렀는데 모듈이 실행되지 않고 멈춰있어요!
- **원인**: RSS 수집 모듈의 중복 방지(Deduplication) 로직으로 인해, 이미 수집했던 뉴스 기사일 경우 자동으로 중단됩니다.
- **해결책**: RSS URL을 다른 카테고리(예: `https://www.yna.co.kr/rss/economy.xml`)로 잠시 변경해 보거나, 모듈을 우클릭하여 `Choose where to start` -> `All`을 선택하여 다시 테스트하세요.

### Q2. OpenAI 모듈에서 `401 Unauthorized` 또는 `429 Rate Limit` 에러가 발생해요.
- **원인**: API Key를 잘못 입력했거나, OpenAI 계정에 결제 수단이 없거나 무료 크레딧이 만료된 경우입니다.
- **해결책**: [platform.openai.com/api-keys](https://platform.openai.com/api-keys)에서 키를 새로 발급받아 Make.com Connection에 재입력하고, Billing 메뉴에서 크레딧 잔액을 확인하세요.

### Q3. Make.com의 Notion 모듈에서 제가 만든 데이터베이스가 안 보여요.
- **원인**: Notion API 연결 권한 설정 시 해당 페이지를 허용하지 않았기 때문입니다.
- **해결책**: Notion 모듈의 Connection을 새로 만들 때 팝업창에서 `Select pages`를 눌러 새로 만든 뉴스 데이터베이스 페이지를 반드시 다시 선택하고 승인하세요.

### Q4. DALL-E 이미지 생성이 시간이 너무 오래 걸리거나 실패해요.
- **원인**: DALL-E 3 모델 요청 프롬프트에 금지어(안전 정책 위반)가 포함되어 있거나 처리 시간이 10초 이상 걸릴 수 있습니다.
- **해결책**: 프롬프트를 영어로 간결하게 설정하고, 모델을 `dall-e-2`로 변경해 테스트해 보세요.

### Q5. 노션에 저장된 3줄 요약 글자가 깨지거나 이상하게 나와요.
- **원인**: Make.com 매핑 시 `Choices[].message.content`가 아닌 다른 이상한 객체 변수를 넣었을 가능성이 큽니다.
- **해결책**: Notion 모듈의 요약 입력 칸을 비우고, OpenAI 모듈의 Output 변수 중 `Text` 또는 `Content` 텍스트 변수를 정확히 찾아 선택하세요.
