# 📘 [term project 수행 가이드] 노코드 AI 뉴스 자동화 파이프라인 팀원별 상세 안내서 (R&R)

> **이 문서의 목적**: AI나 Make.com, Notion 등 지원 도구를 단 한 번도 사용해 본 적 없는 초보자도 화면의 클릭 위치, 버튼 이름, 입력할 프롬프트, 데이터 매핑 방식까지 100% 그대로 따라 하여 뉴스 수집 → AI 3줄 요약 → DALL-E 썸네일 생성 → 감성 분석 → Notion DB 저장 → 에러 처리 파이프라인을 완성할 수 있도록 작성된 **최종 실습 매뉴얼**입니다.

---

## 🔰 1. 초보자를 위한 1분 기초 개념 사전

실습을 시작하기 전, 자주 사용되는 용어를 쉬운 비유로 이해해 둡니다.

| 용어 | 개념 설명 | 쉬운 비유 |
| :--- | :--- | :--- |
| **Make.com** | 코딩 없이 여러 웹 서비스를 연결하여 자동으로 일하게 만드는 '노코드 자동화 도구' | 여러 기능 블록을 조립하는 **디지털 레고 조립판** |
| **모듈 (Module)** | Make.com 안에서 특정 기능(뉴스 수집, AI 요약 등)을 수행하는 개별 블록 | 레고 조립판 위에 올리는 **각각의 레고 블록** |
| **시나리오 (Scenario)** | 모듈들이 화살표로 연결되어 처음부터 끝까지 자동으로 작동하는 전체 흐름 | 완성된 **자동 레고 장치** |
| **RSS 피드** | 뉴스 사이트에서 새 기사가 올라오면 알려주는 구독용 주소 | 매일 아침 집 앞으로 배달되는 **신문 배달 주소** |
| **OpenAI API Key** | Make.com이 ChatGPT나 DALL-E 인공지능을 불러와 쓰기 위한 개인 접속 비밀번호 | AI 서비스를 이용하기 위한 **비밀 열쇠** |
| **프롬프트 (Prompt)** | AI(ChatGPT/DALL-E)에게 작업 내용과 형식을 전달하는 지시문 | AI 직원에게 전달하는 **작업 지시서** |
| **매핑 (Mapping)** | 이전 블록에서 나온 결과물(기사 제목, 내용 등)을 다음 블록의 입력 칸에 쏙 넣어 연결하는 작업 | 이전 부품을 다음 부품의 구멍에 **소켓처럼 꽂는 작업** |
| **Notion 데이터베이스** | 수집 및 요약된 뉴스를 표나 카드로 깔끔하게 정리해 모아두는 공간 | 결과를 차곡차곡 정리하는 **디지털 스크랩북** |

---

## 🔄 2. 전체 자동화 파이프라인 구조

전체 시스템은 5명의 팀원이 역할을 나누어 순차적으로 연결되는 구조입니다.

```text
[팀원 1: 김승현]           [팀원 2: 이태우]            [팀원 3: 한승학]
 1. RSS 뉴스 수집   ──►   2. OpenAI 3줄 요약  ──►   3. DALL-E 썸네일 생성
 2. 키워드 필터링            프롬프트 작성             + 감성 분석 태그
                                                                │
[팀원 5: 최슬기]           [팀원 4: 공미순]                      │
 5. 총괄 QA 검증    ◄──   4. Notion DB에 저장  ◄───────────────┘
    & README 작성          + 에러 핸들러 구성
```

---

## 👥 3. 팀원별 담당 역할 및 1:1 따라하기 매뉴얼

---

### 👤 팀원 1: 김승현 - 데이터 수집 및 조건 필터링 (Data Ingestion & Preprocessing)

> **담당 업무**: 매일 아침 정해진 시간에 뉴스를 자동으로 수집하고, 'AI' 관련 키워드가 포함된 기사만 골라내어 다음 단계로 넘깁니다.

#### 🛠️ 1단계: Make.com 가입 및 새 시나리오 생성
1. 브라우저(Chrome 권장)를 열고 [Make.com](https://www.make.com/)에 접속합니다.
2. 우측 상단의 **[Sign up]** (회원가입) 또는 **[Log in]** (로그인)을 클릭합니다. (구글 계정 간편 가입 추천)
3. 로그인 후 화면 좌측 메뉴바에서 **`Scenarios`** (시나리오) 아이콘을 클릭한 뒤, 우측 상단의 파란색 **`+ Create a new scenario`** (새 시나리오 생성) 버튼을 누릅니다.
4. 하얀 바탕의 넓은 작업 공간(캔버스)이 뜨며, 화면 중앙에 **플러스(+) 모양이 그려진 커다란 보라색/하얀색 둥근 원**이 나타납니다.

#### 🛠️ 2단계: 매일 아침 자동 실행 스케줄러(Schedule) 설정
1. 커다란 플러스(+) 원 왼쪽 아래에 달려 있는 **작은 시계 모양 아이콘(🕒)**을 클릭합니다.
2. 설정 팝업창에서 **Run scenario** (실행 방식) 드롭다운을 클릭하고 **`Every day`** (매일)를 선택합니다.
3. 아래 **Time** (시간) 항목에 `09:00`을 입력합니다. (매일 아침 9시 실행)
4. 하단의 파란색 **[OK]** 버튼을 눌러 설정을 저장합니다.

#### 🛠️ 3단계: RSS 뉴스 수집 모듈 추가 및 주소 입력
1. 화면 중앙의 커다란 **플러스(+) 원**을 클릭합니다.
2. 검색창에 **`RSS`**를 입력하고, 검색 결과에 나오는 주황색 **[RSS]** 아이콘을 클릭합니다.
3. 세부 작업 목록 창이 뜨면 **`Retrieve RSS feed items`** (RSS 피드 항목 가져오기)를 클릭합니다.
4. 캔버스에 주황색 RSS 모듈 아이콘이 생깁니다. 이 아이콘을 마우스로 **더블클릭**합니다.
5. 설정창 항목을 다음과 같이 입력합니다:
   - **URL**: `https://www.yna.co.kr/rss/news.xml` (연합뉴스 최신기사 RSS 피드 주소)
   - **Maximum number of returned items** (가져올 최대 기사 수): **`1`** (테스트 및 비용 절약을 위해 1 입력)
6. 하단의 파란색 **[OK]** 버튼을 클릭합니다.

#### 🛠️ 4단계: 'AI' 키워드 필터(Filter) 설정
1. RSS 모듈 아이콘 오른쪽 끝으로 마우스를 가져가면 작은 **플러스(+) 아이콘**이나 모듈 사이 연결선(점선/실선)이 보입니다.
2. 모듈과 모듈 사이를 잇는 **연결선(또는 렌치 모양 아이콘)**을 클릭합니다.
3. 팝업 메뉴에서 **[Set up a filter]** (필터 설정)를 클릭합니다.
4. 팝업 창에 아래 내용을 입력합니다:
   - **Label** (필터 이름): `AI 키워드 필터링`
   - **Condition** (조건 설정):
     - **왼쪽 입력 칸**: 마우스로 클릭하면 옆에 이전 모듈 데이터 변수 목록 창(Mapping Panel)이 뜹니다. RSS 모듈 아래의 **`Title`** (기사 제목) 변수를 찾아 클릭합니다.
     - **중앙 드롭다운**: Text operators 항목 중 **`Contains (case insensitive)`** (포함 - 대소문자 구문 없음)를 선택합니다.
     - **오른쪽 입력 칸**: **`AI`** 라고 적습니다.
5. (선택사항) '인공지능' 단어도 추가하고 싶다면 하단의 **[Add OR rule]**을 클릭하고, 동일하게 `Title` - `Contains` - `인공지능`을 추가합니다.
6. 하단의 파란색 **[OK]** 버튼을 눌러 완료합니다.

#### 💡 주의사항:
- RSS URL 입력 시 주소 앞뒤에 불필요한 공백(띄어쓰기)이 들어가면 에러가 납니다.
- 처음 테스트할 때는 조건 키워드를 `AI` 1개만 지정하는 것이 에러 방지에 좋습니다.

---

### 👤 팀원 2: 이태우 - AI 요약 & 텍스트 가공 (AI Core Processing)

> **담당 업무**: OpenAI API 키를 발급받아 Make.com에 연결하고, 뉴스 본문을 3줄로 정확히 요약하도록 프롬프트를 구성합니다.

#### 🛠️ 1단계: OpenAI API Key 발급 및 크레딧 확인
1. 브라우저에서 [OpenAI Platform](https://platform.openai.com/)에 접속하여 가입 및 로그인합니다.
2. 좌측 사이드바에서 **[API keys]** (열쇠 모양 아이콘)를 클릭합니다.
3. 화면 중앙의 **`+ Create new secret key`** 버튼을 클릭합니다.
4. Key Name에 `Make-News-Automation`이라 적고 **[Create secret key]**를 클릭합니다.
5. 화면에 나타나는 `sk-proj-...` 형태의 긴 비밀키를 **[Copy]** 버튼을 눌러 복사한 후, **즉시 메모장에 붙여넣어 보관**합니다. (이 창을 닫으면 다시 볼 수 없습니다!)
6. ⚠️ **중요 (결제 잔액 확인)**: OpenAI API는 ChatGPT Plus($20/월) 결제와 별개로 최소 $5의 API 크레딧 충전이 필요합니다. 좌측 메뉴 `Settings` -> `Billing`에서 크레딧 잔액이 남아있는지 확인하세요.

#### 🛠️ 2단계: Make.com에 OpenAI 모듈 추가 및 Connection 연결
1. 팀원 1의 RSS 필터 오른쪽에 있는 **[Add a module]** (`+` 아이콘)을 누릅니다.
2. 검색창에 **`OpenAI`**를 검색하여 선택합니다.
3. 세부 작업 중 **`Create a Chat Completion`** (또는 `Create a Completion`)을 선택합니다.
4. 모듈 설정 창 맨 위의 **Create a Connection** (연결 생성) 버튼을 누릅니다.
5. Connection name에 `My OpenAI Connection`을 입력하고, **API Key**란에 메모장에 복사해 둔 `sk-proj-...` 키를 붙여넣은 뒤 **[Save]**를 클릭합니다.

#### 🛠️ 3단계: AI 3줄 요약 프롬프트 세팅 & 변수 매핑
1. **Model** (AI 모델) 선택란에서 **`gpt-4o-mini`**를 선택합니다. (속도가 빠르고 비용이 저렴함)
2. **Messages** 항목 아래의 **[Add item]** 버튼을 두 번 클릭하여 메시지 2개를 만듭니다.

   - 🔹 **첫 번째 메시지 (System 지시문)**:
     - **Role**: `System`
     - **Message Content**:
       ```text
       당신은 전문 한국어 뉴스 요약 에이전트입니다. 전달받은 기사의 제목과 본문을 읽고, 가장 중요한 내용만 정확히 3줄로 요약하세요. 인사말이나 부연 설명 없이 오직 3줄 요약 텍스트만 출력하세요.
       ```

   - 🔹 **두 번째 메시지 (User 요청문 & 매핑)**:
     - **Role**: `User`
     - **Message Content**:
       ```text
       다음 뉴스 기사를 3줄로 요약해 주세요.

       [기사 제목]: 
       [기사 본문]: 
       ```
     - 📌 **변수 매핑(연결) 방법**:
       - `[기사 제목]:` 바로 뒤에 커서를 놓고, 클릭 시 우측/좌측에 뜨는 팝업 변수 패널에서 **1. RSS 모듈 아래의 `Title` 태그**를 클릭합니다.
       - `[기사 본문]:` 바로 뒤에 커서를 놓고, 팝업 변수 패널에서 **1. RSS 모듈 아래의 `Description` 태그**를 클릭합니다.

3. 하단의 파란색 **[OK]** 버튼을 누릅니다.

#### 💡 주의사항:
- 매핑이 정상적으로 되면 텍스트 칸 안에 `1. Title`과 `1. Description`이라는 주황색 블록 형태 태그가 들어갑니다. 손으로 글자를 입력하는 것이 아니라 클릭해서 태그를 넣어야 합니다.

---

### 👤 팀원 3: 한승학 - 보너스 기능 개발 (DALL-E 썸네일 & 감성 분석)

> **담당 업무**: 뉴스 주제에 어울리는 이미지(썸네일)를 AI로 자동 생성하고, 기사의 감성(긍정/부정/중립)을 분석하는 태그를 추출합니다.

#### 🛠️ 1단계: DALL-E 3 이미지 생성 모듈 세팅
1. 팀원 2의 OpenAI 요약 모듈 오른쪽에 **[Add a module]** (`+` 아이콘)을 누릅니다.
2. **`OpenAI`** 모듈을 선택하고, 작업 목록 중 **`Generate an Image`** (이미지 생성)를 선택합니다.
3. **Connection**: 팀원 2가 만든 `My OpenAI Connection`을 그대로 선택합니다.
4. **Model**: **`dall-e-3`** (또는 `dall-e-2`)를 선택합니다.
5. **Prompt** (이미지 생성 지시문) 입력란에 다음 영문 프롬프트를 복사하여 넣습니다:
   ```text
   Create a modern, sleek technology news thumbnail vector illustration inspired by the news title: 
   ```
   - 프롬프트 맨 끝에 커서를 두고, 변수 패널에서 **1. RSS 모듈의 `Title` 태그**를 클릭해 붙여넣습니다.
6. **Size**: `1024x1024` 선택 후 **[OK]** 버튼을 누릅니다.

> 💰 **DALL-E 비용 주의 및 100% 무료 대체 도구 안내**
> - **DALL-E 결제 비용**: DALL-E 3는 생성 1건당 약 $0.04(약 50원)의 API 결제 크레딧이 차감됩니다. 테스트 실행이 잦거나 비용이 부담스러운 경우 아래의 무료 대체법을 활용하세요.
> - **🎁 100% 무료 대체법 (Pollinations.ai)**:
>   - Make.com에 OpenAI 대신 **`HTTP` -> `Make a request`** 모듈을 추가합니다.
>   - URL에 `https://image.pollinations.ai/prompt/technology%20news%20` + `1. RSS Title` 태그를 넣고 GET 방식으로 호출하면 **API Key, 가입, 카드 결제 없이 100% 무료 AI 이미지 URL**이 바로 생성됩니다.
> - **🎁 무료 스톡 사진 대안 (Unsplash)**:
>   - Make.com의 기본 제공 모듈인 **`Unsplash` -> `Search Photos`**를 추가하면 고화질 무료 사진 주소를 가져와 사용할 수도 있습니다.

#### 🛠️ 2단계: AI 감성 분석(Sentiment Analysis) 모듈 세팅
1. 이미지 모듈 오른쪽에 **[Add a module]**을 눌러 **`OpenAI`** -> **`Create a Chat Completion`**을 하나 더 추가합니다.
2. **Model**: `gpt-4o-mini` 선택.
3. **Messages** 항목 설정:
   - **Item 1**: Role: `System` / Content: `당신은 뉴스 감성 분석기입니다. 입력된 기사를 보고 오직 '긍정', '부정', '중립' 중 단 하나의 단어만 출력하세요. 다른 설명은 절대로 붙이지 마세요.`
   - **Item 2**: Role: `User` / Content: `다음 기사의 감성을 분류하세요: ` (뒤에 **1. RSS 모듈의 `Title`** 태그 매핑)
4. **[OK]**를 눌러 설정을 마칩니다.

#### 💡 주의사항:
- DALL-E 3 이미지 생성은 건당 약 $0.04의 비용이 소요되므로, 무한 실행되지 않도록 주의하고 비용이 걱정된다면 상단의 무료 대체법(Pollinations.ai)을 적용하세요.
- 감성 분석 결과는 노션의 '선택(Select)' 속성에 들어가므로 출력 결과가 오직 `긍정`, `부정`, `중립` 세 단어 중 하나로만 나오도록 프롬프트를 엄격히 지정해야 합니다.

---

### 👤 팀원 4: 공미순 - Notion DB 구축 & 에러 핸들링 (Storage & Reliability)

> **담당 업무**: 수집·가공된 모든 데이터가 저장될 Notion 데이터베이스를 만들고 Make.com과 연결한 뒤, 에러 발생 시 시스템 멈춤을 방지하는 예외 처리를 구성합니다.

#### 🛠️ 1단계: Notion 데이터베이스 표 작성
1. 브라우저에서 [Notion.so](https://www.notion.so/)에 접속하여 로그인합니다.
2. 좌측 메뉴바에서 **`+ 페이지 추가`**를 누릅니다.
3. 페이지 제목에 `🤖 AI 뉴스 자동 요약 보관함`을 입력합니다.
4. 본문 빈 영역에 `/데이터베이스 - 전체 페이지` (또는 `/Database - Full page`)를 입력하고 엔터를 누릅니다.
5. 생성된 표의 상단 열(속성) 헤더를 클릭하여 아래와 같이 속성 이름과 유형을 만듭니다:

| 속성 이름 | 속성 유형 (Property Type) | 설정 방법 |
| :--- | :--- | :--- |
| **제목** (기본) | **제목 (Title)** | 기본 열 이름만 `제목`으로 변경 |
| **요약** | **텍스트 (Text)** | 우측 `+` 클릭 → `텍스트` 선택 → 이름: `요약` |
| **원문 링크** | **URL** | 우측 `+` 클릭 → `URL` 선택 → 이름: `원문 링크` |
| **발행일시** | **날짜 (Date)** | 우측 `+` 클릭 → `날짜` 선택 → 이름: `발행일시` |
| **감성 태그** | **선택 (Select)** | 우측 `+` 클릭 → `선택` 선택 → 이름: `감성 태그` → 옵션값으로 `긍정`, `부정`, `중립` 3개 미리 추가 |
| **썸네일** | **URL** | 우측 `+` 클릭 → `URL` 선택 → 이름: `썸네일` |

#### 🛠️ 2단계: Make.com Notion 모듈 연동 및 1:1 데이터 매핑
1. Make.com 파이프라인 맨 끝에 **[Add a module]**을 누르고 **`Notion`**을 검색해 클릭합니다.
2. 작업 목록 중 **`Create a Database Item`** (데이터베이스 항목 생성)을 선택합니다.
3. **Connection** -> **[Create a Connection]**을 누릅니다.
4. 노션 권한 요청 팝업창이 뜨면 **[페이지 선택 / Select pages]**를 누른 뒤, 방금 만든 `🤖 AI 뉴스 자동 요약 보관함` 체크박스를 켜고 **[페이지 허용 / Allow access]**를 클릭합니다.
5. **Database ID** 항목 클릭 후 목록에서 `🤖 AI 뉴스 자동 요약 보관함`을 선택합니다.
6. 아래 나타나는 속성별 매핑 입력 칸에 변수 태그를 1:1로 클릭해 넣습니다:
   - **제목 (Title)** ──► `1. RSS` 모듈의 **`Title`**
   - **요약 (Text)** ──► `2. OpenAI 요약` 모듈의 **`Choices[].message.content`** (또는 `Result`)
   - **원문 링크 (URL)** ──► `1. RSS` 모듈의 **`URL`** (또는 `Link`)
   - **발행일시 (Date)** ──► `1. RSS` 모듈의 **`Date`**
   - **감성 태그 (Select)** ──► `4. OpenAI 감성분석` 모듈의 **`Choices[].message.content`**
   - **썸네일 (URL)** ──► `3. DALL-E` 모듈의 **`Data[].url`**
7. 파란색 **[OK]** 버튼을 누릅니다.

#### 🛠️ 3단계: 에러 핸들러(Error Handler) 설정
1. OpenAI 모듈이나 Notion 모듈 위에서 **마우스 우클릭**을 합니다.
2. 메뉴에서 **`Add error handler`** (에러 핸들러 추가)를 클릭합니다.
3. 에러 발생 시 처리 방식으로 **`Ignore`** (오류 무시 후 다음 진행) 또는 **`Break`** (일시 보관 후 재시도) directives 아이콘을 연결합니다.

#### 💡 주의사항:
- 노션 속성 이름(`제목`, `요약`, `원문 링크` 등)과 유형(URL, Text, Date 등)이 Make.com 매핑 화면에 보이는 이름과 토시 하나 틀리지 않고 일치해야 데이터가 정상 저장됩니다.

---

### 👤 팀원 5: 최슬기 - 총괄 QA, E2E 테스트 & 최종 문서화 (QA & Technical Writer)

> **담당 업무**: 파이프라인 전체를 수동 실행(`Run Once`)하여 오류 없이 데이터가 들어오는지 최종 검증하고 제출용 README 문서를 정리합니다.

#### 🛠️ 1단계: 전체 파이프라인 수동 시연 테스트 (Run Once)
1. Make.com 화면 좌측 하단 조종판 바에 있는 재생 모양의 **`[Run Once]`** (한 번 실행) 버튼을 누릅니다.
2. 각 모듈 아이콘이 순서대로 회전하며 작동하는지 지켜봅니다.
3. 정상 완료되면 각 모듈 우측 상단에 숫자가 적힌 **둥근 인스펙터 말풍선(Inspector `①`, `②`, `③` 등)**이 생기고 체크표시(✔)가 뜹니다.

#### 🛠️ 2단계: 모듈별 인스펙터 말풍선 클릭 및 데이터 검증
1. **1번 RSS 말풍선 클릭**: `Output` -> `Bundle 1`을 열어 기사 제목과 URL이 잘 긁혀왔는지 확인.
2. **2번 OpenAI 말풍선 클릭**: `Output` -> `Choices` -> `Message` -> `Content`를 열어 3줄 요약 문장이 깨지지 않고 생성되었는지 확인.
3. **5번 Notion 실제 페이지 접속**: 노션 표에 새 행이 생성되면서 제목, 요약, 링크, 날짜, 감성태그, 썸네일 주소가 비어있지 않고 채워졌는지 눈으로 확인.

#### 🛠️ 3단계: 제출 문서 정리 및 보안 마스킹
1. 실행 과정 캡처 이미지나 텍스트에서 **OpenAI API Key(`sk-proj-...`)**, Notion 개인 계정 이메일 등 개인정보를 모자이크 처리합니다.
2. 사용된 RSS 주소와 3줄 요약 프롬프트를 정리하여 최신 README 문서로 만듭니다.

---

## 🛠️ 4. 초보자를 위한 1:1 화면 클릭 위치 가이드

- **Make.com 실행 조종판 (하단 바)**:
  - ▶ **Run Once**: 시나리오 1회 테스트 실행 버튼.
  - 💾 **Save**: 작성한 시나리오 저장 버튼 (자주 클릭 권장).
  - ⚙ **Scheduling (ON/OFF)**: 시나리오 자동 가동 스위치. (테스트 완료 후 ON으로 변경)
- **Make.com 매핑 패널**:
  - 모듈 입력 칸을 클릭하면 화면에 뜨는 알록달록한 태그 창. 이 창에서 이전 모듈 번호(1, 2, 3...)에 해당하는 변수를 선택해야 함.

---

## ❓ 5. 초보자 자주 묻는 질문 & 트러블슈팅 FAQ (TOP 6)

### Q1. `Run Once`를 눌렀는데 1번 RSS 모듈만 돌아가고 다음으로 안 넘어가요!
- **원인**: RSS 모듈의 중복 방지(Deduplication) 기능으로 인해, 이전에 이미 가져온 뉴스를 중복으로 가져오지 않도록 시스템이 자동으로 차단한 것입니다. 에러가 아닙니다!
- **해결책**: RSS 모듈을 **마우스 우클릭** -> **`Choose where to start`** 클릭 -> **`All`** (모든 기사 대상)을 선택하고 **[OK]**를 누른 뒤 `Run Once`를 다시 실행하세요.

### Q2. OpenAI 모듈에서 `401 Unauthorized` 에러가 발생해요.
- **원인**: API Key를 복사할 때 오타가 났거나, 앞뒤에 공백이 포함된 경우입니다.
- **해결책**: [platform.openai.com/api-keys](https://platform.openai.com/api-keys)에서 키를 새로 생성해 메모장에 정확히 복사하고, Make.com Connection의 `Re-connect` 또는 새 Connection을 만들어 다시 붙여넣으세요.

### Q3. OpenAI 모듈에서 `429 You exceeded your current quota` 에러가 발생해요.
- **원인**: OpenAI API 사용 잔액(Credit)이 $0 이어서 호출이 거부된 것입니다. (ChatGPT Plus 유료 구독과 별개)
- **해결책**: OpenAI Platform 사이트 접속 -> `Settings` -> `Billing` -> `Add payment details`에서 최소 $5를 결제 충전한 뒤 사용하세요.

### Q4. Make.com Notion 모듈에서 제가 만든 데이터베이스 이름이 안 보여요.
- **원인**: Notion 연결 생성 시 팝업창 권한 설정에서 새로 만든 페이지의 접근 권한을 허용해주지 않았기 때문입니다.
- **해결책**: Notion 모듈의 Connection 선택란에서 `Create a connection`을 새로 누른 뒤, 팝업창 하단 **`[페이지 선택 / Select pages]`** 버튼을 클릭하여 `🤖 AI 뉴스 자동 요약 보관함` 페이지 체크박스를 켜고 허용하세요.

### Q5. 노션 표의 '요약'이나 '감성 태그' 칸이 비어있는 채로 저장돼요.
- **원인**: Make.com 매핑 시 변수 태그를 잘못 연결했거나, AI 프롬프트 출력이 노션 속성 유형과 일치하지 않는 경우입니다.
- **해결책**: Make.com Notion 모듈 설정 창에서 '요약' 입력란에 OpenAI 모듈의 `Choices[].message.content` 태그가 제대로 박혀있는지 재확인하세요.

### Q6. 실수로 OpenAI API Key를 GitHub이나 블로그에 올렸어요!
- **원인**: API Key 노출 시 다른 사람이 내 크레딧을 무단 사용할 수 있습니다.
- **해결책**: 즉시 OpenAI Platform의 API Keys 메뉴로 이동하여 해당 키 옆의 **쓰레기통 아이콘(Revoke key)**을 눌러 삭제하고 새 키를 발급받으세요.

---

## 🎁 부록: 완성된 청사진(Blueprint) 불러오기 (Import) 안내

팀원이 공유한 Blueprint JSON 파일(`Integration RSS, OpenAI (ChatGPT, Sora, Whisper).blueprint.json`)이 있는 경우, 클릭 몇 번으로 전체 모듈 구조를 한 번에 가져올 수 있습니다:

1. Make.com 새 시나리오 빈 캔버스 화면으로 이동합니다.
2. 화면 하단 조종판 바 메뉴 중 **점 3개 아이콘 (`...` More)**을 클릭합니다.
3. 메뉴 중 **`Import Blueprint`** (청사진 가져오기)를 클릭합니다.
4. 내 컴퓨터에 있는 `.blueprint.json` 파일을 선택하여 열면, 모든 모듈과 연결선이 캔버스 위에 한 번에 배치됩니다.
5. 이후 본인의 OpenAI API Key와 Notion 데이터베이스 연결만 세팅해주면 바로 가동할 수 있습니다.
