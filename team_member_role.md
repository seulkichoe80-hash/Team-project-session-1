# 팀원별 역할 안내서

이 문서는 make.com를 이용해 뉴스 요약 자동화 흐름을 만드는 프로젝트를 설명하기 위한 문서입니다. 불필요한 설명은 줄이고, 각 도구에서 실제로 어떤 작업을 하는지 중심으로 정리합니다.

---

## 1. 프로젝트 목표

이 프로젝트는 다음 흐름을 자동으로 만드는 것입니다.

1. RSS로 뉴스 기사를 가져온다.
2. AI가 기사 내용을 요약한다.
3. 요약 결과를 Notion에 저장한다.
4. 오류가 나면 다시 시도하거나 기록한다.

---

## 2. 전체 흐름

### 1단계: 뉴스 수집
- RSS에서 뉴스 기사를 가져온다.
- make.com에서 RSS 모듈을 사용해 기사 데이터를 받는다.
- 키워드가 포함된 기사만 통과시키도록 필터를 적용한다.

### 2단계: AI 요약
- OpenAI API를 사용해 기사 내용을 3줄로 요약하게 만든다.
- 프롬프트를 통해 요약 문장을 정리한다.
- 결과가 저장에 적합한 형태로 나오도록 확인한다.

### 3단계: Notion 저장
- Notion 데이터베이스에 제목, 요약, 링크, 날짜를 저장한다.
- 저장된 내용이 제대로 보이도록 속성을 맞춘다.

### 4단계: 보너스 기능
- DALL·E로 썸네일 이미지를 만든다.
- AI로 감성 태그를 만든다.
- 생성된 이미지 URL과 감성 태그도 함께 저장한다.

### 5단계: 오류 처리
- RSS가 비었거나 API가 실패하면 재시도한다.
- 실패 시 로그를 남기고 알림을 보낸다.

---

## 3. 팀원별 역할

### 팀원 1: 김승현 - 뉴스 수집과 조건 필터링
이 팀원은 make.com에서 뉴스가 들어오도록 연결하고, 필요한 기사만 남기도록 조건을 설정하는 역할을 담당합니다. 초보자라면 “모듈을 추가하고, 연결하고, 조건을 넣는 것”이 핵심입니다.

실행 방법(최신 UI 기준):
1. 브라우저에서 make.com에 접속한다. 주소는 https://www.make.com/ 이다.
2. 로그인 후 화면 상단이나 왼쪽 메뉴에서 “새 시나리오”, “Create scenario”, 또는 “+” 버튼이 보이는 곳을 찾는다.
3. 시나리오 편집 화면이 열리면, 화면 중앙 또는 상단의 “Add”, “모듈 추가”, 또는 “+” 버튼을 누른다.
4. 검색창에 “Schedule” 또는 “스케줄”을 입력해 트리거 모듈을 추가한다.
5. 다시 모듈 추가 버튼을 눌러 “RSS”를 검색해 RSS 모듈을 추가한다.
6. RSS 모듈의 입력창에 뉴스 피드 주소를 붙여넣는다.
   - 예시 URL: https://www.yna.co.kr/rss/news.xml
7. 같은 방식으로 “Filter” 또는 “필터” 모듈을 추가한다.
8. 필터 조건에서 제목(title) 항목을 선택하고, “contains” 또는 “포함” 조건으로 “AI”, “GPT”, “LLM”을 입력한다.
9. 모듈 사이를 화살표로 연결한다. 스케줄 → RSS → Filter 순서가 기본이다.
10. 실행 버튼을 눌러 테스트한다. 버튼 이름은 “Run”, “Run once”, “실행”처럼 바뀔 수 있다.

이 역할에서 쓰는 지원도구 사용법:
- make.com 사용법: 홈페이지에서 로그인한 뒤, 왼쪽 상단의 “Create a Scenario”를 눌러 새 워크플로우를 만든다. 모듈은 Add 버튼으로 추가하고, 모듈끼리는 드래그로 연결한다.
- RSS 사용법: RSS 모듈에 뉴스 피드 주소를 넣으면 기사 제목, 링크, 발행일 같은 데이터가 흐른다. 이 데이터를 다음 단계로 넘기려면 출력값을 연결한다.
- Filter 사용법: 필터 모듈에서 조건을 넣으면 원하는 기사만 다음 단계로 넘어간다. 초보자는 키워드를 2~3개만 넣는 것이 실수 없이 시작하는 방법이다.

주의할 점:
- RSS 링크가 잘못되면 뉴스가 하나도 들어오지 않는다.
- 조건을 너무 넓게 잡으면 원하지 않는 기사까지 통과한다.
- 처음에는 “AI” 하나만 넣고 테스트한 뒤, 다른 키워드를 추가하는 것이 좋다.

### 팀원 2: 이태우 - AI 요약과 텍스트 가공
이 팀원은 OpenAI API를 연결해 기사 내용을 요약하게 만들고, 결과를 저장하기 좋은 형태로 정리하는 역할을 담당합니다. 초보자는 “API 연결”과 “프롬프트 입력”이 가장 중요합니다.

실행 방법(최신 UI 기준):
1. OpenAI 웹사이트에 접속한다. 주소는 https://platform.openai.com/ 이다.
2. 우측 상단의 “Sign up”, “Log in”, 또는 프로필 아이콘을 눌러 로그인한다.
3. 로그인 후 왼쪽 메뉴나 상단의 프로필 영역에서 “API keys” 또는 “API” 메뉴를 찾는다.
4. “Create new secret key” 또는 “새 비밀 키 생성”을 누른다.
5. 생성된 키를 복사한다. 이 키는 다시 보이지 않을 수 있으므로 바로 메모장에 저장한다.
6. make.com으로 돌아와 모듈 추가 버튼을 누른다.
7. “OpenAI”를 검색해 모듈을 추가한다.
8. “Connect”, “Create a connection”, 또는 “연결 생성”처럼 보이는 버튼을 누른다.
9. 복사한 API 키를 붙여넣고 연결한다.
10. 작업 종류에서 “Create a chat completion”, “챗 생성”, 또는 유사한 옵션을 선택한다.
11. 모델을 선택하고, 아래 프롬프트를 입력한다.

예시 프롬프트:
```text
시스템 프롬프트:
당신은 한국어 뉴스 요약 전문가입니다. 기사 내용을 정확하게 파악한 뒤, 3줄 이내로 핵심만 요약하세요.

사용자 프롬프트:
제목: {제목}
본문: {본문}

요약 결과는 다음 형식으로 출력하세요:
- 줄 1: 핵심 내용
- 줄 2: 영향 또는 의미
- 줄 3: 한 줄 요약
```

12. 제목과 본문 값이 이전 모듈에서 넘어오도록 연결한다.
13. 결과가 너무 길면 “3줄 이내”, “핵심만”, “한글로”를 추가해 프롬프트를 수정한다.

이 역할에서 쓰는 지원도구 사용법:
- OpenAI API 키 발급: https://platform.openai.com/api-keys 에서 키를 생성한다. UI는 “Create new secret key” 버튼이 보이는 구조이다.
- OpenAI API 문서 확인: https://platform.openai.com/docs/api-reference 에서 요청 형식과 모델의 기본 사용법을 참고한다.
- make.com 연결: OpenAI 모듈에 생성한 키를 붙여넣고, “Create a connection”으로 연결한다.
- 프롬프트 작성: 요약 결과를 3줄 이내로 제한하는 문장을 직접 넣는다.

주의할 점:
- API 키는 공개된 문서나 채팅방에 올리지 않는다.
- 프롬프트가 길면 AI가 불필요한 문장을 추가할 수 있다.
- 처음에는 “뉴스를 3줄로 요약해줘”처럼 아주 짧게 테스트하는 것이 좋다.

### 팀원 3: 한승학 - 보너스 기능 개발
이 팀원은 기본 흐름에 이미지 생성과 감성 분석 기능을 더하는 역할을 담당합니다. 초보자라면 “모듈을 하나 더 붙이는 느낌”으로 접근하면 쉽습니다.

실행 방법(최신 UI 기준):
1. make.com에서 다시 모듈 추가 버튼을 누른다.
2. “OpenAI”를 검색해 모듈을 추가한다.
3. 화면에서 이미지 생성 또는 이미지 관련 작업을 선택한다. 버튼 이름은 “Image”, “Generate image”, “이미지 생성”처럼 바뀔 수 있다.
4. OpenAI 연결이 되어 있다면 같은 연결을 사용하고, 없다면 API 키를 다시 연결한다.
5. 이미지 생성용 프롬프트를 입력한다.

예시 이미지 프롬프트:
```text
AI 뉴스 기사에 어울리는 기술 감성의 썸네일 이미지를 생성해줘.
배경은 파란색과 흰색을 사용하고, 로봇 또는 스마트 기기 느낌으로 표현해줘.
```

6. 감성 분석도 같은 방식으로 추가한다.
7. 기사 본문을 넣고 “긍정”, “부정”, “중립” 중 하나만 나오도록 프롬프트를 입력한다.

예시 감성 분석 프롬프트:
```text
다음 뉴스 기사 내용을 읽고 감성을 분류해줘.
결과는 반드시 하나만 출력해줘: 긍정, 부정, 중립

기사 내용:
{본문}
```

8. 생성된 이미지 URL과 감성 결과를 다음 단계로 전달한다.
9. 이후 Notion 저장 단계로 연결한다.

이 역할에서 쓰는 지원도구 사용법:
- DALL·E 연동: OpenAI의 이미지 생성 기능을 이용한다. 주소는 https://platform.openai.com/docs/guides/images 이다.
- UI 흐름: make.com에서 OpenAI 모듈을 추가한 뒤, “Generate image” 또는 유사한 이미지 생성 작업을 선택한다.
- 결과 전달: 생성된 이미지 URL은 다음 모듈로 넘기고, 감성 태그는 Notion 속성에 저장한다.

주의할 점:
- 이미지 생성은 텍스트 생성보다 시간이 조금 더 걸릴 수 있다.
- 프롬프트가 너무 길면 결과가 복잡해진다.
- 감성 분석은 “긍정/부정/중립” 중 하나로 나오게 짧게 만드는 것이 좋다.

### 팀원 4: 공미순 - Notion 저장과 에러 처리
이 팀원은 make.com과 Notion을 연결해 결과를 저장하고, 문제가 생기면 재시도하거나 알림을 보내는 역할을 담당합니다. 초보자라면 “Notion 데이터베이스 만들기”와 “모듈 연결”이 핵심입니다.

실행 방법(최신 UI 기준):
1. Notion 웹사이트에 접속한다. 주소는 https://www.notion.so/ 이다.
2. “Sign up” 또는 “회원가입”을 눌러 이메일 또는 구글 계정으로 가입한다.
3. 가입 후 새 워크스페이스를 만든다.
4. 왼쪽 사이드바에서 “New”, “새 페이지”, 또는 “+” 버튼을 눌러 페이지를 만든다.
5. 페이지 안에서 “Database”, “데이터베이스”, 또는 “+ New database” 같은 메뉴를 찾아 만든다.
6. 데이터베이스에 다음 속성을 추가한다.
   - 제목: Title
   - 요약: Text
   - 원문 링크: URL
   - 발행일시: Date
   - 감성 태그: Select
   - 썸네일: URL
7. 데이터베이스를 열고 상단 주소 표시줄 또는 공유 메뉴에서 URL을 복사해 둔다.
8. make.com으로 돌아와 모듈 추가 버튼을 누르고 “Notion” 모듈을 추가한다.
9. “Connect”, “Create a connection”, 또는 “연결 생성”처럼 보이는 버튼을 누른다.
10. Notion 계정으로 로그인하고, 권한 허용 화면에서 “Allow access” 또는 “허용”을 누른다.
11. 데이터베이스 목록이 보이면, 방금 만든 데이터베이스를 선택한다.
12. 제목, 요약, 링크, 날짜, 감성 태그, 썸네일 값을 각각 매핑한다.
13. 오류 처리 모듈을 추가해 RSS가 비었거나 API가 실패하면 재시도하도록 설정한다.

이 역할에서 쓰는 지원도구 사용법:
- Notion 가입 및 설치: https://www.notion.so/ 에서 회원가입하고, 데스크톱 앱은 https://www.notion.so/desktop 에서 설치할 수 있다.
- Notion UI 사용법: 왼쪽 메뉴에서 페이지를 만들고, 페이지 안에서 Database를 생성한다. 속성은 오른쪽 패널에서 추가할 수 있다.
- Notion 연결: make.com의 Notion 모듈에서 “Create a connection”을 선택하고, 권한 허용 후 데이터베이스를 선택한다.
- 에러 처리: 실패가 발생하면 재시도 경로를 추가해 자동으로 다시 실행되게 만든다.

주의할 점:
- 속성 이름이 틀리면 저장이 실패한다.
- Notion 데이터베이스 URL은 페이지 주소를 그대로 사용한다.
- 값이 비어 있으면 항목 생성이 실패할 수 있다.

### 팀원 5: 최슬기 - QA, 테스트, 문서화
이 팀원은 흐름이 실제로 실행되는지 확인하고, 제출할 문서를 정리하는 역할을 담당합니다. 초보자에게는 “한 번 실제로 돌려 보고 확인하는 과정”이 매우 중요합니다.

실행 방법(실제 화면 기준):
1. make.com에서 “Run once” 또는 “한 번 실행” 버튼을 누른다.
2. 실행 결과 패널에서 각 모듈이 정상적으로 지나갔는지 확인한다.
3. 뉴스가 들어오는지, AI가 요약을 만드는지, Notion에 저장되는지 순서대로 확인한다.
4. 문제가 있으면 어떤 모듈에서 멈췄는지 확인한다.
   - RSS에 값이 안 들어오는지
   - OpenAI가 응답하지 않는지
   - Notion 저장이 실패하는지
5. 결과를 스크린샷이나 메모로 남긴다.
6. README에 사용한 RSS 주소, 프롬프트 예시, Notion 데이터베이스 주소, 오류 처리 방식을 정리한다.

이 역할에서 쓰는 지원도구 사용법:
- make.com 실행 테스트: 시나리오 하단의 “Run once” 버튼을 눌러 실제 실행 결과를 확인한다.
- 결과 확인: 각 모듈의 출력값과 오류 메시지를 확인해 어디서 멈췄는지 찾는다.
- 문서 정리: 사용한 URL, 프롬프트, 연결 정보, 오류 해결 내용을 기록한다.

주의할 점:
- 테스트는 실제 실행 후에만 의미가 있다.
- 실행 결과가 없으면 설정이 잘못된 것일 수 있다.
- 작은 기능부터 테스트한 뒤 점점 기능을 추가하는 것이 좋다.

---

## 4. 지원 도구별 초보자용 따라하기

이제 아래 항목은 각 팀원의 역할 설명에서 이미 다룬 내용을 다시 정리한 보조 설명입니다. 초보자는 팀원별 설명을 먼저 읽고, 필요할 때 아래 내용을 참고하면 됩니다.

### 4-1. make.com에서 모듈 추가하는 법
1. make.com에 로그인한다. 주소는 https://www.make.com/ 이다.
2. 상단 또는 사이드바에서 “새 시나리오”, “Create scenario”, 또는 “+” 버튼을 눌러 새 워크플로우를 만든다.
3. 화면 중앙 또는 상단의 “Add”, “모듈 추가”, 또는 “+” 버튼을 누른다.
4. 검색창에 원하는 도구 이름을 입력한다. 예: RSS, Schedule, OpenAI, Notion, Filter
5. 원하는 모듈을 선택하면 시나리오에 추가된다.
6. 모듈을 클릭하면 오른쪽 또는 하단에 설정 패널이 나타난다.
7. 필요한 값(예: URL, API 키, 데이터베이스 주소)을 입력한다.
8. 모듈과 모듈 사이를 화살표로 연결한다.
9. 버튼 이름은 서비스 업데이트에 따라 조금씩 바뀔 수 있으므로, 화면에 보이는 핵심 단어로 찾으면 된다.

### 4-2. OpenAI API를 make.com에 연결하는 법
1. OpenAI 사이트에 접속한다. 주소는 https://platform.openai.com/ 이다.
2. 로그인 후 https://platform.openai.com/api-keys 에서 키를 생성한다.
3. 생성한 키를 복사해 메모장에 저장한다.
4. make.com에서 OpenAI 모듈을 추가한다.
5. “Create a connection”을 누른다.
6. 복사한 API 키를 붙여넣는다.
7. 연결이 완료되면 모델을 선택하고, 프롬프트를 입력한다.

### 4-3. DALL·E를 make.com에 연동하는 법
1. OpenAI의 이미지 가이드 페이지를 확인한다. 주소는 https://platform.openai.com/docs/guides/images 이다.
2. make.com에서 OpenAI 모듈을 추가한다.
3. 이미지 생성 작업을 선택한다.
4. 프롬프트를 입력하고, 생성된 이미지 URL을 다음 단계로 연결한다.

### 4-4. Notion 가입 및 설치 방법
1. Notion 웹사이트에 접속한다. 주소는 https://www.notion.so/ 이다.
2. 회원가입 후 워크스페이스를 만든다.
3. 데스크톱 앱은 https://www.notion.so/desktop 에서 설치할 수 있다.
4. 페이지를 만들고 데이터베이스를 추가한 뒤 속성을 만든다.

### 4-5. Notion을 make.com과 연결하는 법
1. make.com에서 Notion 모듈을 추가한다.
2. “Create a connection”을 누른다.
3. Notion 계정으로 로그인하고 권한을 허용한다.
4. 데이터베이스를 선택한 뒤, 제목/요약/링크/날짜/감성 태그/썸네일 값을 매핑한다.

### 4-6. 실제 실행하는 법
1. 시나리오를 모두 연결한 뒤 “Run once”를 누른다.
2. 오른쪽 패널에서 실행 결과를 확인한다.
3. 오류가 있으면 빨간색 표시를 확인하고, 오류 메시지를 읽는다.
4. 문제가 해결되면 다시 실행해 본다.

---

## 5. 초보자용 실행 순서

1. make.com에 로그인한다.
2. 새 시나리오를 만든다.
3. Schedule 모듈을 추가한다.
4. RSS 모듈을 추가한다.
5. RSS 주소를 입력한다.
6. Filter 모듈을 추가해 키워드를 설정한다.
7. OpenAI 모듈을 추가해 요약 프롬프트를 넣는다.
8. Notion 모듈을 추가해 데이터베이스에 저장한다.
9. DALL·E와 감성 분석 기능을 추가한다.
10. 오류 처리 모듈을 넣는다.
11. “Run once”로 테스트한다.
12. 문제가 있으면 하나씩 수정한다.

---

## 6. 참고할 점

- 처음부터 모든 기능을 한 번에 만들지 말고, 먼저 기본 흐름부터 만든다.
- 모듈은 “하나씩 추가하고, 하나씩 연결”하는 방식으로 진행한다.
- API 키나 토큰 같은 민감한 정보는 노출하지 않는다.
- 오류 메시지가 나오면 어디에서 멈췄는지 확인한다.
- 결과가 마음에 들지 않으면 프롬프트를 바꿔본다.
- 테스트가 잘 되면 다음 기능을 추가하는 것이 가장 안전하다.

