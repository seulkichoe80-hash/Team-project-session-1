# 🤖 노코드 자동화 파이프라인: AI 뉴스 요약 시스템

> **외부 RSS 피드**를 통해 최신 기술 뉴스를 자동으로 수집하고, **생성형 AI(OpenAI)**를 통해 핵심 내용을 3줄로 요약한 뒤, **Notion Database**에 자동으로 아카이빙하는 무중단 노코드 자동화 파이프라인 프로젝트입니다.

---

## 📋 1. 프로젝트 개요
* **프로젝트 명**: AI 뉴스 요약 및 자동 아카이빙 시스템
* **사용 툴**: 
  * **자동화 툴**: Make (Integromat)
  * **AI 모델**: OpenAI GPT (`gpt-5.4-mini`)[cite: 2]
  * **저장소**: Notion Database[cite: 2]
  * **알림 연동**: Google Gmail (예외 처리 알림)[cite: 2]
* **목표**: 정보 수집부터 AI 가공, 협업 툴 저장에 이르는 전 과정을 자동화하여 반복 수작업을 없애고 안정적인 에러 핸들링 체계를 구축합니다.

---

## 👥 2. 팀 역할 및 개인별 작업 요약 (R&R)

| 팀원 이름 | 담당 역할 | 주요 수행 업무 |
| :--- | :--- | :--- |
| **김승현** | 데이터 수집 & 조건 필터링 | • 연합뉴스 RSS 피드 연동 및 수집 설정<br>• AI/인공지능 키워드 직관적 필터링 조건 설계<br>• 중복 수집 방지 및 최신 항목 제어 |
| **이태우** | AI Prompt & 구조화 | • OpenAI GPT 기반 3줄 요약 프롬프트 설계<br>• 감성 분석 및 태그 자동 분류 체계 구축<br>• Parse JSON을 통한 구조화된 데이터 매핑 |
| **한승학** | 보너스 기능 및 부가 가공 | • DALL-E 기반 썸네일 이미지 자동 생성 검토<br>• 멀티모달 데이터 확장 연계 기획 |
| **공미순** | 노션 DB & 에러 핸들링 | • Notion 데이터베이스 속성 설계 및 매핑<br>• Make `On Error` 라우터 및 최대 2회 재시도 정책 수립<br>• 오류 발생 시 Gmail 자동 알림 연동 |
| **최슬기** | 총괄 QA & 최종 검증 | • End-to-End 통합 테스트 수행<br>• 제약사항 및 API 비용/보안 마스킹 검수<br>• 최종 README 및 기획서 문서화 |

---

## ⚙️ 3. 주요 기능 및 구현 상세

### ① 데이터 수집 및 주제 필터링 (김승현)
* **소스 URL**: 연합뉴스 RSS (`https://www.yna.co.kr/rss/news.xml`)[cite: 2]
* **필터링 기준**: 
  * 기사 제목(`title`) 또는 본문(`description`) 내에 **`AI`, `인공지능`, `LLM`, `GPT`, `Machine Learning`, `딥러닝`** 키워드가 포함된 항목만 선별합니다[cite: 2].
  * 관련 없는 일반 뉴스의 무분별한 AI API 호출을 원천 차단하여 비용을 절감합니다.

### ② AI 가공 및 텍스트 요약 (이태우)
* **모델 선택**: `gpt-5.4-mini`[cite: 2]
* **프롬프트 전략**:
  * **System Prompt**: 기술 뉴스 요약 전문가로서 기사를 정확히 3줄로 요약하고, 외부 공백 없이 순수 JSON 형식(`summary`, `sentiment`, `tags`)으로만 응답하도록 강제[cite: 2].
  * **User Prompt**: 기사 본문을 최대 3,000자로 슬라이싱(`substring`)하여 토큰 초과 오류를 방지하고 핵심 사실 위주로 가공[cite: 2].

### ③ Notion DB 자동 저장 (공미순)
* **Notion Database 속성 매핑 구조**:
  * **제목 (`title`)**: 기사 원문 제목 (`{{1.title}}`)[cite: 2]
  * **카테고리 (`select`)**: `AI`[cite: 2]
  * **원문링크 (`url`)**: 기사 원본 링크 (`{{1.url}}`)[cite: 2]
  * **발행일시 (`date`)**: 기사 생성일 (`{{1.dateCreated}}`, 시간 제외 설정)[cite: 2]
  * **요약문 (`rich_text`)**: AI가 파싱한 3줄 요약 배열 (`{{4.summary}}`)[cite: 2]

---

## 🛡️ 4. 에러 처리 정책과 선택 이유

* **재시도(Retry) 및 예외 처리 정책**:
  * Make 시나리오 내 모듈별 최대 재시도 2회 상한선 준수.
  * OpenAI API 호출 또는 노션 전송 과정에서 네트워크 장애 및 파싱 오류 발생 시, `On Error` 라우터를 통해 즉시 흐름이 중단되지 않고 예외 처리 경로로 진입하도록 설계[cite: 2].
* **실패 알림 자동화**:
  * 최종 에러 발생 시 지정된 관리자 이메일(`seulkichoe80@gmail.com`)로 에러 로그 사유가 담긴 Gmail 발송 알림 연동 완료[cite: 2].
  * **선택 이유**: 사람이 모니터링하지 않는 야간이나 주말에도 시스템 중단 여부를 즉시 파악하고 대응할 수 있는 무중단 시스템을 구축하기 위함입니다.

---

## 📸 5. 워크플로우 구조 스크린샷 (예시 흐름)

```text
[1] RSS 피드 수집 (매일 정기 실행)
         ↓
[2] 주제 필터링 Router (AI, LLM 등 키워드 매칭)
         ↓
[3] OpenAI - 3줄 요약 및 JSON 생성 (gpt-5.4-mini)[cite: 2]
         ↓
[4] Parse JSON (데이터 구조화)[cite: 2]
         ↓
[5] Notion DB - 페이지 생성 및 속성 매핑[cite: 2]
         │
   (❌ 에러 발생 시)
         ↓
[6] On Error → Gmail 자동 에러 알림 전송[cite: 2]