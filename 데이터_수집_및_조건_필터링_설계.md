# 📡 데이터 수집 & 조건 필터링 상세 설계서 (Data Ingestion & Preprocessing Design)

> **프로젝트명**: [Project B] 뉴스 요약 자동화 워크플로우  
> **담당자**: 팀원 1 (김승현)  
> **참고 워크플로우 블루프린트**: `Integration RSS, OpenAI (ChatGPT, Sora, Whisper).blueprint.json` (Make.com)

---

## 📌 1. 개요 및 담당 범위 (Overview & Scope)

본 설계서는 자동화 파이프라인의 **첫 번째 단계인 데이터 수집(Data Ingestion) 및 전처리/필터링(Preprocessing & Filtering)**에 대한 기술 명세서입니다.

* **핵심 역할**: 외부 기술 뉴스 RSS 피드 자동 수집, 스케줄링 설정, 동일 기사의 중복 수집 방지, 지정된 주제(AI/Dev)에 부합하는 뉴스의 라우팅 필터 구축
* **설계 목적**: 무관한 뉴스 수집 차단 및 1차 중복 제거를 통해 파이프라인 후속 단계(OpenAI 가공, Notion 저장)로 깨끗한 데이터만 전달하며, 무의미한 AI API 호출 청구 비용을 100% 방지함

---

## 🔄 2. 수집 및 전처리 파이프라인 흐름

```text
[1] 정기 스케줄 트리거 (매일 09:00 AM KST)
         │
         ▼
[2] RSS 최신 기사 수집 모듈 (rss:TriggerNewArticle, Module ID: 2)
         │  ├─ 추출 파라미터: Title, URL(GUID), Description, Date
         │  └─ 1차 중복 방지 (Deduplication State) 자동 검사
         ▼
[3] AI 카테고리 분석 데이터 수신 (json:ParseJSON, Module ID: 8)
         │  └─ {{8.category}} 변수 (AI 또는 Dev/Cloud)
         ▼
[4] 라우팅 & 조건 필터링 (builtin:BasicRouter, Module ID: 4)
         ├── [Route 1] AI 뉴스 필터 ({{8.category}} == 'AI') ──► Discord AI 알림 & Notion DB 저장
         ├── [Route 2] Dev 뉴스 필터 ({{8.category}} == 'Dev/Cloud') ──► Discord Dev 알림 & Google Sheet 저장
         └── [Route Drop] 주제 미부합 뉴스 ──► 자동 걸러냄 (Filter Out / 차단)
```

---

## ⚙️ 3. Make.com 블루프린트 노드별 세부 기술 명세

### 3.1 [모듈 1] RSS 최신 기사 수집 (`rss:TriggerNewArticle`, Module ID: 2)
* **기능**: 등록된 외부 IT/기술 뉴스 RSS 피드 URL에서 최신 작성 기사를 자동으로 가져옴
* **주요 설정 값**:
  * **RSS Feed URL**: 연합뉴스 최신기사 피드 (`https://www.yna.co.kr/rss/news.xml` 등)
  * **Maximum number of returned items**: 1건 ~ 설정 가능 (미션 기준 1회 구동 시 최신 1건 선택)
  * **Execution Schedule**: `Every day at 09:00 AM` (Timezone: `Asia/Seoul`)
* **추출 파라미터 데이터 매핑**:
  * `{{2.title}}` : 뉴스 기사 제목
  * `{{2.url}}` : 뉴스 기사 원문 URL (식별용 GUID로 활용)
  * `{{2.description}}` / `{{2.content}}` : 뉴스 기사 본문 요약 또는 본문 텍스트
  * `{{2.date}}` / `{{2.pubDate}}` : 기사 작성/발행 시각

---

### 3.2 [모듈 2] 중복 저장 방지 로직 (Deduplication Key)
* **동작 매커니즘**:
  * Make의 `rss:TriggerNewArticle` 모듈은 내부 데이터베이스에 최근 수집 처리된 기사의 `GUID` 및 `URL` 식별자 State를 자동으로 기억함
* **중복 방지 정책**:
  * 매일 설정된 시간에 트리거가 구동되더라도, 이미 이전 실행에서 처리되었던 기사는 **1차 수집 단계에서 자동 제외(Skip)** 처리함
* **효과**:
  * 동일 기사가 반복 저장되는 현상을 원천 차단하고, 불필요한 AI 가공 호출 및 DB 저장 오버헤드를 완전 예방함

---

### 3.3 [모듈 3] 주제 라우팅 & 조건 필터링 (`builtin:BasicRouter`, Module ID: 4)
* **동작 매커니즘**:
  * AI 가공 모듈(`json:ParseJSON`, Module ID: 8)에서 정규화된 `{{8.category}}` 변수 값을 전달받아 조건식에 따라 해당 브랜치로만 뉴스 전달
* **세부 필터 조건 명세 (Filter Rules)**:

#### ① AI 주제 브랜치 필터 (Filter Name: `AI`)
* **조건식 (Condition)**: `{{8.category}}` `text:equal` `AI`
* **동작**: `category`가 'AI'인 기사만 조건을 통과시켜 **AI 뉴스 Discord 채널 및 노션 데이터베이스** 저장 파이프라인으로 연결

#### ② 개발/클라우드 주제 브랜치 필터 (Filter Name: `dev`)
* **조건식 (Condition)**: `{{8.category}}` `text:equal` `Dev/Cloud`
* **동작**: `category`가 'Dev/Cloud'인 기사를 선별하여 **개발 전용 Discord 채널 및 구글 시트 Archive** 저장 파이프라인으로 연결

#### ③ 무관 뉴스 차단 (Filter Out)
* **동작**: 설정된 키워드/카테고리 조건(`AI`, `Dev/Cloud`)에 해당하지 않는 일반 기술 뉴스는 라우터 필터 단에서 **자동 드롭(Filter Out)**시켜 이후 알림/저장 모듈이 동작하지 않도록 차단함

---

## 💡 4. 필요성 및 기대 효과 (Design Rationale)

1. **API 비용 청구 방지**:
   * RSS 피드에서 쏟아지는 수백 개의 뉴스 중 주제에 맞는 뉴스를 1차로 거르고 1건만 선별함으로써 무의미한 OpenAI API 토큰 비용 소모 방지
2. **무중단 정시 자동화**:
   * 타임존(Asia/Seoul) 기준 매일 09:00 AM 정시 구동 스케줄로 사람의 개입 없는 무인 데이터 수집 구현
3. **데이터 완결성 확보**:
   * GUID/URL 기반 중복 저장 방지 키 적용으로 데이터베이스의 중복 레코드 생성을 완벽 차단

---

## 📂 5. 연동 블루프린트 파일
* [Integration RSS, OpenAI (ChatGPT, Sora, Whisper).blueprint.json](file:///d:/코디세이/Team-project-session-1/Integration%20RSS,%20OpenAI%20(ChatGPT,%20Sora,%20Whisper).blueprint.json)
